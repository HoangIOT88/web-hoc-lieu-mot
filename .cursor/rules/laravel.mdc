---
description: 
globs: 
alwaysApply: true
---

 [README.md](mdc:README.md)

mysql  Ver 9.2.0 for macos15.2 on arm64 (Homebrew)
Composer version 2.8.5 2025-01-21 15:23:40
PHP version 8.2.28 (/opt/homebrew/Cellar/php@8.2/8.2.28/bin/php)
PHP 8.2.28 (cli) (built: Mar 11 2025 17:58:12) (NTS)
Laravel Version: 10.48.29

  You are an expert in HP, and related web development technologies.

  Core Principles
  - Write concise, technical responses with accurate PHP/Laravel examples.
  - Prioritize SOLID principles for object-oriented programming and clean architecture.
  - Follow PHP and Laravel best practices, ensuring consistency and readability.
  - Design for scalability and maintainability, ensuring the system can grow with ease.
  - Prefer iteration and modularization over duplication to promote code reuse.
  - Use consistent and descriptive names for variables, methods, and classes to improve readability.

  Dependencies
  - Composer for dependency management
  - PHP 8.2+
  - Laravel 10.0+

  PHP and Laravel Standards
  - Leverage PHP 8.2+ features when appropriate (e.g., typed properties, match expressions).
  - Adhere to PSR-12 coding standards for consistent code style.
  - Always use strict typing: declare(strict_types=1);
  - Utilize Laravel's built-in features and helpers to maximize efficiency.
  - Follow Laravel's directory structure and file naming conventions.
  - Implement robust error handling and logging:
    > Use Laravel's exception handling and logging features.
    > Create custom exceptions when necessary.
    > Employ try-catch blocks for expected exceptions.
  - Use Laravel's validation features for form and request data.
  - Implement middleware for request filtering and modification.
  - Utilize Laravel's Eloquent ORM for database interactions.
  - Use Laravel's query builder for complex database operations.
  - Create and maintain proper database migrations and seeders.


  Laravel Best Practices
  - Use Eloquent ORM and Query Builder over raw SQL queries when possible
  - Implement Repository and Service patterns for better code organization and reusability
  - Utilize Laravel's built-in authentication and authorization features (Sanctum, Policies)
  - Leverage Laravel's caching mechanisms (Redis, Memcached) for improved performance
  - Use job queues and Laravel Horizon for handling long-running tasks and background processing
  - Implement comprehensive testing using PHPUnit and Laravel Dusk for unit, feature, and browser tests
  - Use API resources and versioning for building robust and maintainable APIs
  - Implement proper error handling and logging using Laravel's exception handler and logging facade
  - Utilize Laravel's validation features, including Form Requests, for data integrity
  - Implement database indexing and use Laravel's query optimization features for better performance
  - Use Laravel Telescope for debugging and performance monitoring in development
  - Leverage Laravel Nova or Filament for rapid admin panel development
  - Implement proper security measures, including CSRF protection, XSS prevention, and input sanitization

  Code Architecture
    * Naming Conventions:
      - Use consistent naming conventions for folders, classes, and files.
      - Follow Laravel's conventions: singular for models, plural for controllers (e.g., User.php, UsersController.php).
      - Use PascalCase for class names, camelCase for method names, and snake_case for database columns.
    * Controller Design:
      - Controllers should be final classes to prevent inheritance.
      - Make controllers read-only (i.e., no property mutations).
      - Avoid injecting dependencies directly into controllers. Instead, use method injection or service classes.
    * Model Design:
      - Models should be final classes to ensure data integrity and prevent unexpected behavior from inheritance.
    * Services:
      - Create a Services folder within the app directory.
      - Organize services into model-specific services and other required services.
      - Service classes should be final and read-only.
      - Use services for complex business logic, keeping controllers thin.
    * Routing:
      - Maintain consistent and organized routes.
      - Create separate route files for each major model or feature area.
      - Group related routes together (e.g., all user-related routes in routes/user.php).
    * Type Declarations:
      - Always use explicit return type declarations for methods and functions.
      - Use appropriate PHP type hints for method parameters.
      - Leverage PHP 8.2+ features like union types and nullable types when necessary.
    * Data Type Consistency:
      - Be consistent and explicit with data type declarations throughout the codebase.
      - Use type hints for properties, method parameters, and return types.
      - Leverage PHP's strict typing to catch type-related errors early.
    * Error Handling:
      - Use Laravel's exception handling and logging features to handle exceptions.
      - Create custom exceptions when necessary.
      - Use try-catch blocks for expected exceptions.
      - Handle exceptions gracefully and return appropriate responses.

  Key points
  - Follow Laravel’s MVC architecture for clear separation of business logic, data, and presentation layers.
  - Implement request validation using Form Requests to ensure secure and validated data inputs.
  - Use Laravel’s built-in authentication system, including Laravel Sanctum for API token management.
  - Ensure the REST API follows Laravel standards, using API Resources for structured and consistent responses.
  - Leverage task scheduling and event listeners to automate recurring tasks and decouple logic.
  - Implement database transactions using Laravel's database facade to ensure data consistency.
  - Use Eloquent ORM for database interactions, enforcing relationships and optimizing queries.
  - Implement API versioning for maintainability and backward compatibility.
  - Optimize performance with caching mechanisms like Redis and Memcached.
  - Ensure robust error handling and logging using Laravel’s exception handler and logging features.

  ------------

	Technologies & Environment
	•	Backend Framework: Laravel 10.48.29
	•	PHP Version: 8.2.28 (cli)
	•	Database: MySQL (Ver 9.2.0 for macOS 15.2 on ARM64 - Homebrew)
	•	Dependency Manager: Composer v2.8.5
	•	Operating System: macOS (ARM64)

⸻

Project Goal

Implement a fully functional web-based Education Management System with 3 clearly defined user groups:
	•	Regular User (Student)
	•	Content User (Course Manager)
	•	Administrator (System Admin)

⸻

Activity Flow (Detailed Rules)

A. Regular User (Student)

Viewing Courses
	•	User visits the website.
	•	User selects menu “Course List”.
	•	System displays all available courses.
	•	User selects a course to view details (curriculum, schedule, instructors).

Course Registration
	•	User selects a course from the list.
	•	Clicks “Register for Course”.
	•	System checks login state:
	•	If not logged in: Redirect to login page.
	•	If logged in: Confirms registration and returns success/failure notification.

Doing Exercises
	•	User logs in.
	•	Visits “My Courses”.
	•	Chooses a registered course.
	•	Selects “Exercise List”.
	•	Completes an exercise and clicks “Submit”.
	•	System saves submission and updates status.

User Login
	•	User clicks “Login”.
	•	Inputs email and password.
	•	System authenticates:
	•	Success: Redirects to user home.
	•	Failure: Returns error message.

Group Chat
	•	User logs in.
	•	Visits “Group Chat”.
	•	Selects or creates a chat group.
	•	Sends and receives messages in real-time.

⸻

B. Content User (Course Manager)

Create/Delete Courses
	•	Logs into the system.
	•	Navigates to “Course Management”.
	•	Creating a course:
	•	Selects “Add New Course”.
	•	Inputs details (name, description, duration).
	•	Saves course data.
	•	Deleting a course:
	•	Selects course and confirms deletion.
	•	System removes course from database.

Creating Content and Exercises
	•	Selects managed course.
	•	Chooses “Add Content and Exercises”.
	•	Inputs lesson content, exercises, and deadlines.
	•	Saves information to database.

Creating/Deleting Chat Groups
	•	Navigates to “Chat Group Management”.
	•	Creating a group:
	•	Selects “Create New Chat Group”.
	•	Inputs name, description, and adds members.
	•	Deleting a group:
	•	Selects a group, confirms deletion, and removes it from database.

User Approval
	•	Visits “User Approval” section.
	•	Sees a list of pending user registrations.
	•	Approves or rejects user registrations.
	•	Updates user status accordingly.

Posting Lectures
	•	Selects managed course.
	•	Chooses “Post Lecture”.
	•	Uploads lecture files (PDFs, videos, articles).
	•	Confirms and saves to database.

Adding Users
	•	Navigates to “User Management”.
	•	Selects “Add New User”.
	•	Inputs user details (name, email, role).
	•	Confirms and creates user account.

⸻

C. Admin User (System Administrator)

Managing Users
	•	Logs into admin dashboard.
	•	Selects “User Management”.
	•	Views the full list of users.
	•	Performs CRUD actions (Create, Read, Update, Delete) on user data.
	•	Saves changes to the database.

User Statistics & Reports
	•	Selects “User Statistics”.
	•	Views user activity data and statistics in visual charts.
	•	Exports statistics as Excel/PDF reports.

⸻

Database Schema (Detailed ERD Rules)

Entities:

User
	•	id (PK), name, email (unique), password_hash, role (USER, CONTENT_USER, ADMIN), created_at, updated_at

Course
	•	id (PK), name, description, duration, content_user_id (FK User.id), created_at, updated_at

CourseRegistration
	•	id (PK), user_id (FK User.id), course_id (FK Course.id), registered_at

Lecture
	•	id (PK), course_id (FK Course.id), title, description, file_url, uploaded_at

Exercise
	•	id (PK), course_id (FK Course.id), title, content, deadline, created_at

ExerciseSubmission
	•	id (PK), exercise_id (FK Exercise.id), user_id (FK User.id), submission_content, submitted_at, score, graded_at

ChatGroup
	•	id (PK), name, description, created_by (FK User.id), created_at

ChatGroupMember
	•	id (PK), group_id (FK ChatGroup.id), user_id (FK User.id), joined_at

Message
	•	id (PK), group_id (FK ChatGroup.id), sender_id (FK User.id), content, sent_at

UserApproval
	•	id (PK), user_id (FK User.id), content_user_id (FK User.id - reviewer), status (PENDING, APPROVED, REJECTED), reviewed_at

AdminReport
	•	id (PK), generated_by_admin_id (FK User.id), report_type (USER_STATS, COURSE_STATS, ACTIVITY_STATS), file_url, generated_at

⸻

ERD Relationships:
	•	User (1-n) Course: managed by Content User.
	•	User (n-n) Course: via CourseRegistration.
	•	Course (1-n) Lecture.
	•	Course (1-n) Exercise.
	•	Exercise (1-n) ExerciseSubmission.
	•	User (1-n) ExerciseSubmission.
	•	ChatGroup (1-n) ChatGroupMember.
	•	User (n-n) ChatGroup: via ChatGroupMember.
	•	ChatGroup (1-n) Message.
	•	User (1-n) Message.
	•	User (1-n) UserApproval: Reviewer (Content User).
	•	User (1-n) UserApproval: Reviewed User.
	•	User/Admin (1-n) AdminReport.

⸻⸻⸻⸻⸻⸻⸻⸻⸻⸻⸻⸻⸻⸻⸻⸻⸻

AI Development Rules & Recommendations
	•	Structure & Clarity:
Clearly separate each user group’s logic into Laravel Controllers and routes.
	•	Follow Laravel Best Practices:
Use Eloquent ORM, resource controllers, and resourceful routing.
	•	Authentication & Middleware:
Implement Laravel’s built-in authentication (auth) and custom middleware for role-based access (User, Content User, Admin).
	•	Error Handling & Notifications:
Standardize responses using Laravel responses (JSON API or standard Blade views). Clearly handle errors and provide user-friendly feedback.
	•	Use of Laravel Relationships:
Properly define and leverage Eloquent Relationships (hasOne, hasMany, belongsTo, belongsToMany).
	•	Database Migrations & Seeders:
AI must generate comprehensive migrations for all tables and relationships. Use seeders for default roles, test users, and sample courses.
	•	Blade Templates & Frontend:
Clearly structured UI components using Blade for views. AJAX or Livewire recommended for dynamic content updates (chat/messages/submissions).
	•	Testing:
AI should include PHPUnit test-cases for controllers and models.
	•	Security:
Apply Laravel security best practices (validation, sanitization, CSRF protection, password hashing).
  